const numbers = [99, 44, 6, 2, 1, 4, 63, 87, 283, 4, 0];

const selectionSort = (arr) => {
    for (let i = 0; i < arr.length; i++) {
        // let min = i
        // for (let j = 0; j < arr.length; j++) {
        //     if (arr[j] )
        // }
    }
};

selectionSort(numbers);
console.log(numbers);
